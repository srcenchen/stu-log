// Code generated by ent, DO NOT EDIT.

package ent

import (
	"eGZ-stu-log/internal/data/ent/class"
	"eGZ-stu-log/internal/data/ent/grade"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Class is the model entity for the Class schema.
type Class struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// ClassName holds the value of the "className" field.
	ClassName string `json:"className,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ClassQuery when eager-loading is set.
	Edges        ClassEdges `json:"edges"`
	class_grade  *int64
	selectValues sql.SelectValues
}

// ClassEdges holds the relations/edges for other nodes in the graph.
type ClassEdges struct {
	// Grade holds the value of the grade edge.
	Grade *Grade `json:"grade,omitempty"`
	// Student holds the value of the student edge.
	Student []*Student `json:"student,omitempty"`
	// StuLogs holds the value of the stuLogs edge.
	StuLogs []*StuLog `json:"stuLogs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// GradeOrErr returns the Grade value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ClassEdges) GradeOrErr() (*Grade, error) {
	if e.Grade != nil {
		return e.Grade, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: grade.Label}
	}
	return nil, &NotLoadedError{edge: "grade"}
}

// StudentOrErr returns the Student value or an error if the edge
// was not loaded in eager-loading.
func (e ClassEdges) StudentOrErr() ([]*Student, error) {
	if e.loadedTypes[1] {
		return e.Student, nil
	}
	return nil, &NotLoadedError{edge: "student"}
}

// StuLogsOrErr returns the StuLogs value or an error if the edge
// was not loaded in eager-loading.
func (e ClassEdges) StuLogsOrErr() ([]*StuLog, error) {
	if e.loadedTypes[2] {
		return e.StuLogs, nil
	}
	return nil, &NotLoadedError{edge: "stuLogs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Class) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case class.FieldID:
			values[i] = new(sql.NullInt64)
		case class.FieldClassName:
			values[i] = new(sql.NullString)
		case class.ForeignKeys[0]: // class_grade
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Class fields.
func (_m *Class) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case class.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case class.FieldClassName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field className", values[i])
			} else if value.Valid {
				_m.ClassName = value.String
			}
		case class.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field class_grade", value)
			} else if value.Valid {
				_m.class_grade = new(int64)
				*_m.class_grade = int64(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Class.
// This includes values selected through modifiers, order, etc.
func (_m *Class) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGrade queries the "grade" edge of the Class entity.
func (_m *Class) QueryGrade() *GradeQuery {
	return NewClassClient(_m.config).QueryGrade(_m)
}

// QueryStudent queries the "student" edge of the Class entity.
func (_m *Class) QueryStudent() *StudentQuery {
	return NewClassClient(_m.config).QueryStudent(_m)
}

// QueryStuLogs queries the "stuLogs" edge of the Class entity.
func (_m *Class) QueryStuLogs() *StuLogQuery {
	return NewClassClient(_m.config).QueryStuLogs(_m)
}

// Update returns a builder for updating this Class.
// Note that you need to call Class.Unwrap() before calling this method if this Class
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Class) Update() *ClassUpdateOne {
	return NewClassClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Class entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Class) Unwrap() *Class {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Class is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Class) String() string {
	var builder strings.Builder
	builder.WriteString("Class(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("className=")
	builder.WriteString(_m.ClassName)
	builder.WriteByte(')')
	return builder.String()
}

// Classes is a parsable slice of Class.
type Classes []*Class
